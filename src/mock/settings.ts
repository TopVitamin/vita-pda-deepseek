import type { SystemParams } from '@/types';

export const defaultSystemParams: SystemParams = {
  autoSubmitAfterScan: false,
  soundEnabled: true,
  vibrateEnabled: true,
  defaultQtyOne: true,
  autoFocusNext: true,
  showSensitiveStock: false,
  pageSize: 20,
};
